# ArgoCD app definition (see argocd/apps-applicationset.yaml + WEP-0001).
# Two Helm releases from one dir: the controller chart and its CRDs chart,
# each with its own releaseName matching the live flux releases for adoption.
name: envoy-ai-gateway
namespace: envoy-ai-gateway-system
helm:
  # OCI: repoURL carries the full artifact path; chart (basename) +
  # targetRevision stay per the Application spec -- see envoy-gateway.
  - chart: ai-gateway-crds-helm
    repoURL: oci://docker.io/envoyproxy/ai-gateway-crds-helm
    version: v1.0.0
    releaseName: envoy-ai-gateway-system-ai-gateway-crds
  - chart: ai-gateway-helm
    repoURL: oci://docker.io/envoyproxy/ai-gateway-helm
    version: v1.0.0
    releaseName: envoy-ai-gateway-system-ai-gateway
    values: values.yaml
manifests:
  - namespace.yaml
  - network-policy.yaml
